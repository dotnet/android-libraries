name: Log Disk Space
description: Logs disk space usage on the runner
runs:
  using: composite
  steps:
  - name: Log disk space
    shell: bash
    run: |
      echo "=== Disk Space Usage ==="
      df -h
