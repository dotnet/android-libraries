<metadata>
  <!--
    `androidx.compose.animation.internal` is an internal Kotlin package
    (JvmDefaultWithCompatibility helper); it is not public API, so remove it
    entirely.
  -->
  <remove-node path="/api/package[@name='androidx.compose.animation.internal']" />
</metadata>