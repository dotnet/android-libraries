@using System.Linq
{
  "solution": {
    "path": "..\\AndroidX.sln",
    "projects": [
@foreach (var dep in @Model.NuGetDependencies)
{
    if (dep.IsProjectReference)
    {
        <text>"@($"{dep.MavenArtifact.MavenGroupId}.{dep.MavenArtifactConfig.GetGeneratedArtifactId()}\\\\{dep.MavenArtifact.MavenGroupId}.{dep.MavenArtifactConfig.GetGeneratedArtifactId()}.csproj")",</text>
    }
}
"@($"{Model.MavenGroupId}.{Model.GetGeneratedArtifactId()}\\\\{Model.MavenGroupId}.{Model.GetGeneratedArtifactId()}.csproj")"
    ]
  }
}
